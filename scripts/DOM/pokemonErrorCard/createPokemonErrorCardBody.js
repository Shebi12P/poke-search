export const createPokemonErrorCardBody = (errorName) => {
    const pokemonErrorCardBody = 
    `<div class="card-body">
        <p>${errorName}</p>
    </div>`;

    return pokemonErrorCardBody;
}